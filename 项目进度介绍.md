# 教学预警系统(学生错题-知识点智能分析体)项目进度介绍

> 文档日期：2026-08-17 | 当前版本：**v0.4.1（前后端分离 · 功能增强版）**
> 前序版本：v0.3（Flask + 模板单页，已废弃但 CLI 入口兼容保留）
> 协作开发规范请参见同目录 [AGENTS.md](AGENTS.md)

---

## 一、项目概述

**项目名称**：教学预警系统 / 学生错题-知识点智能分析体

**项目定位**：面向高校程序设计类课程（当前为《Java 程序设计》）的**学情数据分析与教学预警平台**。

**核心目标**：将智慧树平台导出的多类学习数据（头歌实验、MOOC 成绩、随堂测验、单元练习、课堂活动、Word 题目）自动解析、聚合分析，识别**班级层面的薄弱环节**与**学生个体的薄弱知识点**，生成可直接用于教学干预的预警报告，并配套 AI 教学助手辅助教师解读数据。

**一句话概括**：上传数据 → 自动分析 → 输出「班级预警 + 个人画像 + 易错知识点排行」→ AI 辅助解读。

**架构升级说明（v0.3 → v0.4）**：
- v0.3 使用 **Flask + Jinja2 模板**（单页单应用，前后端代码耦合）；
- v0.4 升级为 **FastAPI（Uvicorn 异步）+ Vue3（Vite 构建）+ TypeScript 严格模式**，前后端彻底分离，开发效率、UI 响应、AI 流式对话体验全面提升。Flask 代码已全部移除（仅 `main.py` CLI 入口保留兼容）。

---

## 二、项目背景与需求

### 2.1 数据来源（智慧树平台导出）

| 数据类型 | 文件格式 | 当前规模（实测） | 说明 |
|---|---|---|---|
| 头歌实验 | CSV | **18 份**（苏理工-Java 程序设计 2026） | 每份一个实验，含 final_score 与 task1~taskN 子任务细分得分 |
| 随堂测验 | XLSX | **60 份**（苏理工软件 / 校区软件 1-3 班 / 校区软件 4-5 班，每班 20 份，覆盖项目 0 基础语法 ~ 项目 7 数据库全系列） | 含班级参与详情、学生得分与正确率、试题详情 |
| MOOC 班级成绩 | CSV | 12 份 | 班级成绩单：单元练习 / 综合测试 / 互评作业 / 课程讨论 / 签到 / 视频等 |
| 单元练习 / 课堂活动 | XLSX | 若干 | 智慧树导出的另两类成绩明细 |
| Word 题目文档 | DOCX | 若干 | 综合测试 / 阶段练习 / 课堂练习题目 |
| 章节对应表 | XLSX | 1 份 | 「智慧慕课-慕课章节对应表」，用于自动构建知识点库 |

### 2.2 教师痛点（v0.4 已全部解决的真实反馈）

1. 数据分散在多个平台、多种格式，人工整理统计耗时；
2. 只看总分无法定位「到底是哪个知识点没掌握」；
3. 缺乏学生个体维度的纵向画像，难以因材施教；
4. 缺少对班级共性薄弱环节的自动预警，发现问题滞后；
5. **2026.08 本轮新增痛点**（已全部修复）：
   - CSV 上传全报「格式错误」（实际是前端 action 路径错导致 404 → 笼统文案误导）
   - 覆盖模式不清空历史报告、同名文件反复上传互相覆盖（随堂测验 4-5 班 20 份只加了 3 份）
   - 报告页切 tab 卡（每次扫盘 2921 文件）、「工具类_3531862」误归到学生个人 tab
   - 跨班/跨年重名学生成绩互相污染聚合
   - 学习建议模板「一对一辅导最薄弱 3 个单元」老师没时间执行，要求删除
   - 顶栏无依据 Tag / 对话无法删除 / 图标白屏 / 对话统计不实时等 UI 问题
   - **2026.08.17 补充修复与增强**：
     - `start.bat` 启动乱码：CMD 子窗口不继承 chcp 65001 且 UTF-8 bat 解析有 bug，已改为 GBK 稳定模式。
     - 上传页 UI 优化：按钮固定在拖拽框下方，不再随列表滑动；增加带滚动条的文件详情表。
     - 吞文件 Bug 修复：修复 `safe_name` 导致头歌实验名提取失败的正则，并补齐了 `experiment_results` 的元信息（safe_name/class_name），确保跨班同名实验不被 merge 误杀。
     - **数据管理增强**：数据文件页面新增「批量删除」与「全部清空」功能；优化复选框颜色，解决白色背景下难以辨认的问题。
     - **报告展示优化**：学生报告命名从混乱的平台字符串统一规范为 `姓名_学号`；报告中心实现学生维度去重聚合，避免同一个学生因多次实验产生冗余行。

### 2.3 系统价值

- **自动化**：一键批量分析全部实验/班级，自动生成报告；
- **细粒度**：分析下钻到子任务、考核项、知识点、个体学生；
- **可解释**：报告直指「哪个实验 / 考核项 / 知识点薄弱、薄弱到什么程度、涉及哪些学生」；
- **智能化**：AI 教学助手基于**真实分析上下文**（班级清单 / 年份清单 / 实验清单 / 每个学生带班级标签）答疑，杜绝凭空编造数据。

---

## 三、系统架构（v0.4）

```
┌─────────────────────────── 数据层(智慧树导出) ───────────────────────────┐
│ 头歌实验CSV │ MOOC班级CSV │ 随堂测验XLSX │ 单元练习/课堂活动 │ Word题目   │
└────────────────────────────────────┬──────────────────────────────────────┘
                                     ▼
┌─────────────────────────── 分析层(analysis/，与Web解耦，CLI可独立跑) ──────┐
│  touge_parser.py     mooc_parser.py    quiz_parser.py   extra_parser.py   │
│  question_parser.py  knowledge_builder.py                                │
│  analyzer.py · subtask_mapper.py(子任务→知识点映射)                       │
│  student_analyzer.py     cross_analyzer.py(跨实验聚合+班级split_key隔离)  │
│  reporter.py                                                              │
└────────────────────────────────────┬──────────────────────────────────────┘
                                     ▼
┌─────────────────────────── 后端服务层(FastAPI + Uvicorn) ────────────────┐
│  server/api/v1/*         —— 8 个路由模块（files/reports/students/chat/   │
│                              conversations/config/context/health）       │
│  server/services/*       —— upload_service / analysis_service /          │
│                              chat_service（业务逻辑，路由不直接写代码）   │
│  server/core/*           —— config / state（进程内存+快照）/ utils（      │
│                              make_student_key + 对话持久化50上限trim）    │
│  server/models/schemas.py—— Pydantic 响应模型                            │
└────────────────────────────────────┬──────────────────────────────────────┘
                              HTTP / JSON（SSE 流式对话）
                                     ▼
┌─────────────────────────── 前端应用层（Vue3 + Vite + TS） ────────────────┐
│  7 个页面：Dashboard · Upload · Reports · Students · Chat · Files ·       │
│            Settings                                                        │
│  src/api/*.ts           —— 后端调用统一封装（组件禁止直接 axios/fetch）   │
│  src/layouts/BasicLayout —— 顶栏+左侧侧栏主布局                           │
│  src/utils/request.ts    —— axios 封装（/api 前缀、10min 超时、错误提示）│
└──────────────────────────────────────────────────────────────────────────┘
```

**技术栈**：
- 后端：Python 3.10+ / FastAPI 0.111 / Uvicorn / pydantic v2 / openpyxl / pandas / python-docx
- 前端：TypeScript 5 / Vue 3.4 / Vite 5 / Element Plus 2.7 + 18 个白名单图标（详见 AGENTS.md §1.2）/ UnoCSS（原子类）
- AI：DeepSeek 官方 API（`https://api.deepseek.com/v1/chat/completions`，model=`deepseek-chat`，**纯流式 SSE**）
- 存储：**纯本地磁盘**（`server/uploads/`、`server/config/*.json`、`server/data/conversations/*.json`），零第三方数据库依赖，一键启动零配置。

---

## 四、核心功能与完成进度（v0.4）

| # | 功能模块 | 状态 | 说明（含本轮 v0.4 修复要点） |
|---|---|---|---|
| 1 | 知识点库自动构建 | ✅ 已完成 | 从「智慧慕课-慕课章节对应表.xlsx」提取教学层次/知识领域/MOOC 单元/项目/视频知识点，去重后生成 `data/knowledge/knowledge_base.csv` |
| 2 | 头歌实验分析 | ✅ 已完成 | 均分/最低最高分/低分率(<60)、子任务得分率、**薄弱子任务(得分率<70%)**、查答案率、实验→知识点映射 |
| 3 | MOOC 班级分析 | ✅ 已完成 | 线上成绩构成（单元练习 50/综合测试 20/互评作业 20/课程讨论 10）、得分率短板识别、课程等级分布、**不及格学生名单**、附加统计（视频/签到/讨论） |
| 4 | 随堂测验分析 | ✅ 已完成 | XLSX 解析、班级正确率、**薄弱学生识别（正确率<70%）**、试题详情统计；**本轮修复：1-3 班+4-5 班同名 xlsx 不覆盖不丢弃**（safe_name 相对路径编码 + merge 组合键判重，60 份实测 0 丢失） |
| 5 | 单元练习 / 课堂活动分析 | ✅ 已完成 | 兼容智慧树两类导出版式，统一输出结构；**本轮修复：启发识别时优先带文件夹级班级名** |
| 6 | Word 题目解析与知识点匹配 | ✅ 已完成 | 支持顺序段落式 / 编号式两种题目格式，自动匹配知识点库 |
| 7 | 学生个人画像 | ✅ 已完成 | 遍历学生全部子任务得分，得分率<70% 标记薄弱，按知识点单元聚合，生成个人报告；**本轮修复：学习建议删除「一对一辅导最薄弱 3 个单元」，只保留「谈话确认出勤 + 家长沟通」两条** |
| 8 | 跨实验学生聚合 | ✅ 已完成 | 多实验合并后按学生维度全量分析，输出**高频易错知识点 TOP 排行**与个人综合报告；**本轮修复：split_key = sid#C#班名 实现跨班/跨年重名学生 100% 成绩隔离**，聚合顶层新增 `all_class_names / all_detected_years / all_experiment_names` 三个数组注入 AI 上下文 |
| 9 | 汇总预警报告 | ✅ 已完成 | 仪表盘 / 报告页一键分类浏览（综合汇总 / 单文件 / 学生个人 / 题目）；**本轮增强：学生个人报告按姓名_学号统一命名，并实现学生维度的去重聚合显示**；前端本地过滤 + 后端 30s 缓存，切 tab <2ms；分类黑名单 60+ 排除非人名项。 |
| 10 | 数据上传页 | ✅ 已完成 | 合并 / 覆盖两种模式；**本轮修复 3 个致命 bug**：① action 路径改 `/api/upload`（之前 404→笼统格式错）② `submitUpload` 自己组 FormData 一次批量提交所有文件（含 `files/files[]/file` 三字段+`relative_paths[]` 带文件夹层级）+ axios 10 分钟超时 ③ 页面下方 3 块 alert 展示：主信息 / skipped 重复 / rejected 校验失败明细，不再笼统报错 |
| 11 | 覆盖模式语义澄清 | ✅ 已完成 | **真清空**：`server/uploads/*` 全删 + `server/uploads/reports/*.txt/*.csv` 全删 + `state.file_hashes.clear()` + 内存全清空 + `_save_analysis_snapshot()` + 报告列表缓存清；UI 文案明确写「覆盖模式（清空所有已上传文件与历史报告，重跑）」 |
| 12 | 上传查重逻辑 | ✅ 已完成 | **本轮修复：md5 存在就跳（逻辑纠正）**；skipped 明细写：`safe_name（内容已分析重复跳过；已存在：xxx）`；随堂测验 60 份实测 skipped=0（不同班级内容字节级不同，md5 自然不重复） |
| 13 | Web 仪表盘 | ✅ 已完成 | 文件上传、自动识别数据类型、批量结果页、单报告 / 个人报告查看与下载 |
| 14 | AI 教学助手 | ✅ 已完成 | DeepSeek 流式 SSE；**本轮 5 项改造**：① 顶部删「DeepSeek · SSE 流式 / 流式就绪」两 Tag ② 删 dashboard 无依据「服务正常」Tag ③ 删「对话:N」统计（不实时，没人看）④ 左侧每条对话右侧加 MoreFilled「⋯」菜单：重命名（PATCH）+ 删除（DELETE）⑤ 聚合上下文注入班级清单/年份清单/实验清单 + 每个学生带 `[班:|年:]` 标签，支持「2026 年软件 2 班成绩总体咋样」直接命中 |
| 15 | 多对话管理 | ✅ 已完成 | 对话创建 / 重命名 / 置顶 / 删除；**存储位置明确：`server/data/conversations/conv_{uuid}.json`（后端磁盘，不是浏览器 localStorage）**，最多保留 50 个，trim 自动清理最旧；换电脑/清浏览器缓存不丢 |
| 16 | 数据文件管理页 | ✅ 已完成 | 独立 `/files` 页面：全量列表、单文件/批量勾选删除、**全部清空一键重置**；**UI 增强**：加深复选框边框颜色，提高白色背景辨识度。 |
| 17 | 学生画像搜索 | ✅ 已完成 | 搜索框 placeholder 从「姓名/学号」升级为「姓名/学号/班级/年份」；后端命中级联 `class_names / detected_years` 集合 |
| 18 | 系统设置页 | ✅ 已完成 | 薄弱阈值 / 低分线 / 查答案率关注线 / DeepSeek API Key 全部真写入 `server/config/settings.json`；API Key 提交后前端显示「已配置\*\*\*\*」不回发明文字符串；**本轮修复：API 已配置后提交空值不覆盖**（保留原 Key） |
| 19 | 交付稳定性保障 | ✅ 已完成 | 分析写操作并发 `ANALYSIS_LOCK`（进程级 RLock）、`/api/healthz` 健康检查、分析快照 save/restore（启动自动恢复上次分析结果，无需每次重新上传）、知识库自动备份（保留最近 20 份）、会话历史上限治理（50 个自动 trim）、统一日志目录（按天轮转）、上传内容校验（MD5 去重 / 空文件 / XLSX PK 头 / CSV 编码归一化 UTF-8）、依赖版本锁定 `requirements.txt` |

**结论：v0.4 核心链路 100% 打通，且本轮用户反馈 18 条痛点全部修复并通过真实数据回归验证。**

---

## 五、版本演进时间线

| 时间 | 版本 | 里程碑 |
|---|---|---|
| 2026-06-30 | v0.1（雏形） | 基于教材知识点库（物理/化学实验 JSON），对错题 CSV 做关键词匹配，产出早期 `analysis_report.txt` / `teaching_warning_report.txt` |
| 2026-07-01 | v0.2（核心引擎） | 转向大学 Java 课程：新增头歌/MOOC 解析器、章节对应表自动建库、统一分析引擎与报告生成，CLI 支持 5 个子命令，产出全量 `summary_report.txt` |
| 2026-07-09 | v0.3（Web 化） | **Flask Web 界面**：上传即分析、学生画像、跨实验聚合、随堂测验/单元练习/课堂活动解析、Word 题目匹配、DeepSeek AI 教学助手、多对话管理 |
| 2026-07-21 | v0.3+ | 适配智慧树**新数据结构**（头歌实验_2026.7.21 目录、校区/苏理工随堂测验目录），新增 quiz 批量分析 |
| 2026-08-07 | v0.3+（收尾） | 交付前稳定性收尾：数据文件独立管理页（搜索/删除）、并发安全锁、`/healthz` 健康检查、知识库自动备份、会话历史上限治理、统一日志目录、上传内容校验、依赖版本锁定 |
| **2026-08-17** | **v0.4（前后端分离，本轮最终交付）** | **FastAPI + Vue3 + Vite + TS** 重写全栈；修复 18 条真实用户痛点（CSV 全格式失败根因、随堂测验 60 份 merge 只增 3 份根因、报告页切 tab 卡顿+工具类误分类+跨班重名污染、学习建议删除一对一、AI 教学助手 UI 5 项改造、图标白名单稳定版、顶栏无依据 Tag 删除、对话重命名/删除菜单） |

---

## 六、当前数据规模与实测输出（2026.08.17 最后一次真实回归）

### 6.1 实测规模

**头歌实验（苏理工 2026）覆盖模式真实上传**：
- CSV 文件：18 份
- 结果：results=18 / skipped=0 / rejected=0
- 学生聚合：62 名学生
- 生成报告：1215 份 txt（含汇总、单实验、学生个人）
- 报告页 4 次连续 API 调用耗时：69.36 ms（第一次含扫盘，后续 30s 内命中缓存 <2 ms）

**随堂测验 3 文件夹 Merge 真实上传**：
- XLSX 文件：60 份（苏理工软件 20 / 校区软件 1-3 班 20 / 校区软件 4-5 班 20）
- 结果：每一步 results=20 / skipped=0 / rejected=0
- 聚合后 quiz_results：20 → 40 → **60**（一个没丢，之前 bug 是 23，根因已修复）
- 学生总数：61 → 166 → **253**（+87，与 4-5 班人数吻合）
- 磁盘 uploads/ 文件计数：20 → 40 → **60**（safe_name 编码：`随堂测验__校区软件4-5班__项目0-Java语言概述.xlsx`，不再覆盖）

### 6.2 汇总预警样例（2026-07-01 实测）

**头歌实验侧**（低分率 Top）：
- `Java 工具类 4-银行账户管理之异常类与集合框架`：96 人，均分 90.56，低分率 **10%**；
- `Java 高级特性 2-多线程技术`：96 人，均分 89.71，低分率 9%，**薄弱子任务 1 个**；
- 其余实验低分率多在 2%~6%，整体掌握情况良好。

**MOOC 班级侧**：
- `2022 春 Java 程序设计`：**互评作业 / 课程讨论**得分率偏低，不及格 **12/126 人（10%）**，为当前最高；
- `2023 秋苏理工计科`：互评作业偏低，不及格 7/89 人（8%）；
- 多数班级存在**「课程讨论」得分率<70%** 的共性短板；
- 2021 春 / 2022 春 / 2023 春多个班级均有不及格警告（1%~6%）。

> 预警逻辑：头歌实验按低分率排序输出 Top 20；MOOC 按短板考核项+不及格率输出警告；全部报告实时生成可下载。

---

## 七、技术要点与经验沉淀（含本轮新增）

1. **知识映射三层策略**：实验名关键词匹配 → 教学层次回退匹配 → 子任务名称规则拆分（`知识点:案例` / `技术点 - 类名`），兼顾不同命名习惯；
2. **编码兼容**：MOOC / 头歌 CSV 自动探测 UTF-8/GBK，兼容智慧树导出差异，归一化写成 UTF-8（无 BOM）存盘，下游 analyzer 统一读；
3. **数据健壮性**：空值 / `--` / 负分（`-1` 视为 0）统一清洗；聚合按 **safe_name + 班级+主键 + 班级+实验名 + 纯文件名 4 重指纹交集**判重，支持**增量合并 100% 不丢文件**；
4. **随堂测验同名不丢 = 三层组合防御**：① 前端 submitUpload 提交 `webkitRelativePath[]` 带文件夹层级 ② 后端 `_safe_name_from_rel` 把 `/` 全局替换成 `__`（避免逐字符分支误保留）③ merge 判重 4 重指纹交集；
5. **跨班重名成绩 100% 隔离**：aggregate_students 顶层新增 `_make_split_key(base_sid, class_names)`：有班级信息时用 `sid#C#排序后第一班名`，无则保持原 sid（**不破坏老 2921 份报告的 safe_key 路径兼容**）；
6. **报告页切 tab 零延迟 = 双层过滤**：前端 onMounted 拉一次 `type=all`，切 tab 用 computed 本地过滤；后端 `_LIST_CACHE["all"]` 单 entry 30s 缓存，所有 type 从内存 filter（不再重复扫盘）；
7. **报告分类误判三重防线**：中文黑名单（60+ 术语）+ 姓名长度 2~4 字 + 双向正则「姓名+学号紧邻 / 学号+姓名紧邻」严格校验，工具类_3531862 100% 排除；
8. **AI 安全约束**：系统提示明确「缺少数据必须如实告知、严禁编造学生成绩」+ 上下文强制注入班级/年份/实验清单枚举；避免大模型幻觉污染教学结论；
9. **稳定 SSE 流式**：前端 chat 页面使用 fetch + ReadableStream 做 SSE（不依赖 axios），后端 `StreamingResponse` 纯文本逐 chunk 推送；
10. **前端零配置**：`start.bat` 一键完成 依赖安装 → 建知识库 → 同时启动前端 dev + 后端 uvicorn；教师端零手工配置。

### 已知待完善点（开发备注）

- 随堂测验 / 单元练习 / 课堂活动三个 xlsx 解析器现在按文件名启发匹配分类，若文件夹名和文件名完全不包含「单元练习/课堂活动/项目」启发词，仍可能被当随堂测验尝试解析（失败后 has_error=True 兜底，用户体验层面不报错，只是不进入列表，后续可以扩展后缀级字段名识别）；
- MOOC 编码探测 UTF-8/GBK 均失败时按 UTF-8 回退读取（已打 warning 日志），极端老导出文件可能仍有乱码，需人工留意；
- AI 教学助手「班级清单」枚举最多 60 条、「实验清单」25 条，如果以后班级/实验规模超过这个上限，会截断（取 Top 最新修改的），后续考虑做成向量级检索替代穷举注入。

---

## 八、下一步计划（建议）

1. **全量回归与精度打磨**：跑通全部 18 头歌实验 + 60 随堂测验 + 12 MOOC 班，统计解析失败文件（如有）逐一修复；
2. **阈值扩展**：薄弱阈值、低分线、查答案率关注线已支持全局配置（settings.json），后续可扩展为「按班级 / 按实验分别配置」；
3. **AI 能力增强**：支持上传题目原文（xlsx/docx）自动匹配知识点并生成讲解；开放「为什么推荐这个知识点」的推理链展示；
4. **自动化闭环**：定时增量拉取新数据；预警 → 干预 → 复测对比的效果追踪；
5. **多教师与权限**：班级 / 数据隔离、教师账号与报告归属；
6. **部署形态**：单机打包（exe）或内网服务器，适配无 Python 环境的教师机；或 Docker 镜像部署。

---

## 九、项目结构速览（v0.4 真实目录）

```
D:\A教育agent\
├── AGENTS.md                  # ★ 本项目开发Agent协作规范（所有写代码AI必读）
├── 项目进度介绍.md             # 本文件
├── 测试说明.txt               # 老师/用户端测试说明
├── start.bat                  # 一键启动（装依赖→建知识库→启动前后端，默认前端5173，后端8000）
├── main.py                    # CLI 入口（兼容保留：build-knowledge / touge / mooc / quiz / summary）
├── requirements.txt           # Python 依赖已锁定版本
├── server/                    # FastAPI 后端
│   ├── app.py                 #   app 实例、CORS、异常/中间件
│   ├── main.py                #   uvicorn 启动入口
│   ├── api/v1/                #   8 个路由：files/reports/students/chat/conversations/config/context/health
│   ├── services/              #   3 个业务服务：upload / analysis / chat
│   ├── models/schemas.py      #   Pydantic 响应模型
│   ├── core/                  #   config / state（进程内存+快照）/ utils / logging
│   ├── uploads/               #   safe_name 唯一化磁盘副本 + reports/（1215+ txt）
│   ├── config/                #   settings.json（阈值+API Key）+ analysis_snapshot
│   └── data/conversations/    #   对话 JSON（50 上限自动 trim）
├── frontend/                  # Vue3 + Vite 前端
│   ├── src/
│   │   ├── api/               #   5 个后端调用封装（组件禁止直接 axios/fetch）
│   │   ├── views/             #   7 个页面：dashboard/upload/reports/students/chat/files/settings
│   │   └── layouts/BasicLayout.vue
│   └── vite.config.ts         #   /api 代理到后端 8000
├── analysis/                  # 纯数据分析（与 Web 解耦，CLI 可用）17 个模块
├── data/knowledge/            # knowledge_base.csv（知识点库，章节对应表自动构建）
├── 头歌/                      # ★ 原始真实测试数据源
│   └── tougeall/智慧树头歌学生学习数据/
│       ├── 头歌实验_2026.7.21/苏理工-Java程序设计（2026）/   # 18 个 CSV（上传格式测试金标准）
│       └── 随堂测验/                                              # 60 个 XLSX（merge 不丢测试金标准）
├── MOOC/                      # 12 份班级成绩 CSV
├── word+excel+pdf/            # 章节对应表（知识点库来源）+ Word 题目模板 + 课程文档
└── output/                    # CLI 时代生成的汇总/单文件报告（兼容保留）
```

---

## 十、使用方式（v0.4）

```powershell
# 方式一：一键启动（推荐老师使用）
start.bat
# → 自动装依赖 → 建知识库（如未建）→ 启动后端 8000 + 前端 5173
# → 浏览器自动打开 http://localhost:5173

# 方式二：命令行独立分析（开发/批量回归用）
python main.py build-knowledge                  # 从章节对应表生成知识点库
python main.py touge  --dir  "头歌/.../头歌实验_2026.7.21"   # 批量头歌实验
python main.py mooc   --dir  MOOC               # 批量 MOOC 班级
python main.py quiz   --dir  "头歌/.../随堂测验" # 批量随堂测验
python main.py summary                          # 生成全量汇总预警报告 → output/summary_report.txt

# 方式三：手动分别启动前后端（开发时用，分别看日志）
# 后端（另开一个 PowerShell）
cd server ; $env:PYTHONPATH='d:\A教育agent;d:\A教育agent\server' ; python main.py
# 前端（另开一个 PowerShell）
cd frontend ; npm.cmd run dev
```

---

*本文件由项目代码与实测数据整理生成，后续版本迭代请同步更新本文档与 [AGENTS.md](AGENTS.md) 协作规范。*
